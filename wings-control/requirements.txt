# wings-control 依赖清单

# Web 框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP 客户端
httpx==0.25.2
requests>=2.31.0

# 序列化
orjson>=3.9.0

# 配置
python-dotenv==1.0.0

# RAG 加速
fschat>=0.2.36

# 以下包仅在引擎容器中使用，sidecar 不需要安装：
# ray, torch, pynvml, torch-npu
